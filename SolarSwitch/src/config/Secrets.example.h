#pragma once

// Credentials. Copy this file to Secrets.h and fill in your values.
// Secrets.h is gitignored and must never be committed.

// ---- WiFi (MKR WiFi 1010 builds) ------------------------------------------
#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-password"

// ---- SolarEdge monitoring API v2 (SolarEdge builds) -----------------------
#define SOLAREDGE_ACCOUNT_KEY "your-account-key"
#define SOLAREDGE_API_KEY "your-api-key"
